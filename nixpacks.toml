[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]
cacheDirectories = ["/root/.cache/pip", "/root/.cache/huggingface"]

[start]
cmd = "python main.py"
